# Behavioral tests

These are specification-style test scenarios for the `audit` skill. They
are not executable unit tests; they are review fixtures used to validate
that routing and protocol behavior stays consistent as the skill evolves.

Each scenario file defines:

- input situation
- expected routing/coverage/persistence decision
- expected user-visible behavior
- prohibited behavior
